---
date: 2025-10-22
author: Mauro Ciappina
---

# Bitácora diaria — 2025-10-22

## 🛠 Actividades realizadas

- [x] T-AUTH-CLERK-01.5.3: Creación del Módulo y Controlador de Webhook.
  - [x] Generado `WebhooksModule` y `ClerkController`.
  - [x] Verificado que `WebhooksModule` se importó automáticamente en `AppModule`.
  - [x] Backend construido con éxito, confirmando la correcta integración de la estructura.
- [x] T-AUTH-CLERK-01.5.4: Implementación de la Lógica de Verificación y Procesamiento.
  - [x] Implementado el endpoint `POST /webhooks/clerk` y la lógica de verificación de Svix en `ClerkController`.
- [x] T-AUTH-CLERK-01.5.5: Habilitar `rawBody` en NestJS.
  - [x] Habilitado `rawBody: true` en `main.ts`.
- [x] Corrección de errores de ESLint en archivos de prueba.
  - [x] Renombrado `module` a `testModule` en `apps/backend/src/webhooks/clerk/clerk.controller.spec.ts`.
- [x] Resolución de problemas de Git.
  - [x] Commit fallido por `pre-commit` hook (ESLint).
  - [x] Commit fallido por `commitlint` (scope inválido).
  - [x] `git push` fallido por falta de upstream.
  - [x] Creación de PR, merge y borrado de rama remota (manual por el usuario).
  - [x] Actualización de `develop` local y borrado de rama local.

## 🔍 Problemas / Bloqueos

- `ERR_PNPM_FETCH_404` para `@types/svix` durante la instalación de dependencias. Resuelto instalando solo `svix` ya que incluye sus propios tipos.
- Archivo `.env` ignorado por `.gitignore`, impidiendo su commit. Se decidió no commitear credenciales sensibles.
- La herramienta `github.create_pull_request` no está disponible directamente, requiriendo la creación manual del PR.

## 🚀 Próximos pasos

- T-AUTH-CLERK-01.5.6: Pruebas Locales con `ngrok` (Opcional, para desarrollo).

## 🧠 Notas / decisiones / aprendizajes

- Es crucial verificar la ruta de ejecución de los comandos `git` cuando se trabaja con submódulos.
- Siempre revisar las reglas de `commitlint` para asegurar la consistencia de los mensajes de commit.
- La importancia de verificar si los paquetes de tipos (`@types/`) son realmente necesarios o si la librería ya los incluye.
- No commitear archivos `.env` con credenciales sensibles.
