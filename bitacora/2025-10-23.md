---
date: 2025-10-23
author: Mauro Ciappina
---

# Bitácora diaria — 2025-10-23

## 🛠 Actividades realizadas

- [x] Creación de la rama `feature/T-BE-GAME-01-game-task-reward-modules`.
- [x] Generación de módulos, controladores y servicios para `games`, `tasks` y `rewards`.
- [x] Definición de interfaces TypeScript para `Game`, `Task`, `Reward` y `UserTask` en `packages/common-types`.
- [x] Implementación de esquemas de Mongoose para `Game`, `Task` y `UserTask`.
- [x] Implementación de lógica de negocio CRUD en `GamesService`, `TasksService` y `RewardsService`.
- [x] Implementación de endpoints CRUD en `GamesController`, `TasksController` y `RewardsController`.
- [x] Inicio de la integración de comunicación asíncrona con eventos:
  - [x] Instalación de `@nestjs/event-emitter`.
  - [x] Actualización de `tasks.module.ts` para importar `EventEmitterModule` y `UserTaskSchema`.
  - [x] Actualización de `tasks.service.ts` para inyectar `EventEmitter2` y `UserTask` model, y añadir método `completeTask`.
  - [x] Actualización de `rewards.module.ts` para importar `MongooseModule` para `Task`.
  - [x] Actualización de `rewards.service.ts` para inyectar el modelo `Task` y escuchar el evento `task.completed`.
- [x] Inicio de la depuración de tests unitarios:
  - [x] Actualización de `games.service.spec.ts` con tests CRUD.
  - [x] Identificación de errores en los tests.
  - [x] Corrección de errores en `rewards.service.ts` (`userTask.reward` undefined).

## 🔍 Problemas / Bloqueos

- Problemas iniciales al crear la rama debido a cambios locales sin confirmar y a la ejecución de comandos Git desde el directorio incorrecto.
- Errores en los tests unitarios relacionados con la importación de `@nestjs/event-emitter`, tipos `null` en Mongoose y el uso de `findByIdAndRemove` (obsoleto).

## 🚀 Próximos pasos

- Continuar con la depuración de los tests unitarios, corrigiendo los errores restantes en `games.service.ts` y `games.service.spec.ts`.
- Implementar los tests unitarios para `TasksService` y `RewardsService`.

## 🧠 Notas / decisiones / aprendizajes

- La importancia de la ruta absoluta al ejecutar comandos en subdirectorios.
- La necesidad de manejar correctamente los tipos `null` en las consultas de Mongoose.
- La actualización de métodos de Mongoose deprecados (ej. `findByIdAndRemove` a `findByIdAndDelete`).
