---
date: 2025-10-15
author: Mauro Ciappina
---

# Bitácora diaria — 2025-10-15

## 🛠 Actividades realizadas

- [x] **T-CI-01: Configurar Pipeline de CI con GitHub Actions.**
  - [x] **T-CI-01.1:** Creación del archivo de workflow `.github/workflows/ci.yml`.
  - [x] **T-CI-01.2:** Definición de los jobs de `lint`, `test` y `build` con triggers optimizados para `push` y `pull_request` a `main` y `develop`.
  - [x] **T-CI-01.3:** Configuración de reglas de protección de ramas en GitHub para requerir que el check `CI` pase antes de mergear (manual o vía `gh` CLI con `run_shell_command`).
  - [x] **T-CI-01.4:** Realización de commit, push y creación de PR.

- [x] **T-CI-01.5:** Verificación de Tipos (TypeScript).
  - Propósito: Asegurar la consistencia de tipos en todo el proyecto TypeScript, detectando errores de compilación antes del despliegue.
  - Cómo: Añadido un paso en `ci.yml` que ejecuta `pnpm --filter frontend tsc --noEmit` y `pnpm --filter backend tsc --noEmit`.

- [x] **T-CI-02: Mejoras Continuas del Pipeline CI.**
  - [x] **T-CI-02.1:** Enforzar Formato y Linting en CI.
    - Propósito: Asegurar que el código en las ramas principales siempre cumpla con los estándares de formato y estilo, incluso si se omiten las verificaciones locales.
    - Cómo: Añadido un paso en `ci.yml` que ejecuta `pnpm format --check` y `pnpm lint`.
  - [x] **T-CI-02.2:** Escaneo de Vulnerabilidades en Dependencias (Dependabot).
    - Propósito: Identificar y mitigar automáticamente vulnerabilidades conocidas en las dependencias del proyecto.
    - Cómo: Configurado Dependabot en la configuración del repositorio de GitHub para escanear dependencias y crear Pull Requests automáticos.
  - [x] **T-CI-02.3:** Notificaciones de Eventos Clave (Telegram).
    - Propósito: Mantener al equipo informado sobre eventos críticos del CI/CD, como la creación de PRs a `develop`, fallos en el pipeline o despliegues.
    - Cómo: Configurado el uso de una GitHub Action (`appleboy/telegram-action`) para enviar mensajes a Telegram en eventos relevantes.

## 🔍 Problemas / Bloqueos

- Ninguno.

## 🚀 Próximos pasos

- Continuar con la Fase 3: Desarrollo del Backend (NestJS).
- Revisar y ajustar la ubicación de las tareas de "Soporte Técnico", "Promoción", "PayPal", "Google Analytics" e "Integración Social" en el `plan-ejecutivo-detallado.md` según su criticidad para el MVP.

## 🧠 Notas / decisiones / aprendizajes

- Se ha completado la configuración inicial del pipeline de CI/CD, incluyendo linting, type-checking, build y tests de backend, así como notificaciones de Telegram y Dependabot.
- Se ha identificado la necesidad de configurar el script `test` en el `package.json` raíz y añadir un job de test para el frontend en el CI. Se ha creado una issue en GitHub para esto.
