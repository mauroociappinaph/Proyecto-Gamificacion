---date: 2025-10-21author: Mauro Ciappina---

# Bitácora diaria — 2025-10-21

## 🛠 Actividades realizadas

- [x] T-BE-SETUP-05.1: Configuración de Ably y Creación del Servicio.
  - [x] Añadida ABLY_API_KEY a .env
  - [x] Instalado el paquete `ably`
  - [x] Creado `ably.module.ts` y `ably.service.ts`
  - [x] Importado `AblyModule` en `AppModule`
  - [x] Backend construido con éxito
- [x] T-AUTH-CLERK-01.3: Integración en el Backend (NestJS).
  - [x] Instalado el SDK de Clerk para Node.js (`@clerk/clerk-sdk-node`).
  - [x] Creado `clerk-auth.guard.ts` y `public.decorator.ts`.
  - [x] Actualizado `clerk.strategy.ts` para validar JWTs de Clerk de forma segura.
  - [x] Actualizado `auth.module.ts` para usar la nueva estrategia y `ConfigModule`.
  - [x] Actualizado `app.module.ts` para configurar `ConfigModule.forRoot()`.
  - [x] Actualizado `main.ts` para aplicar `ClerkAuthGuard` globalmente.
- [x] T-BE-SETUP-05.2: Implementar Endpoint de Autenticación de Tokens.
  - [x] Creado `ably.controller.ts` con el endpoint `/ably/token` protegido por `ClerkAuthGuard`.
  - [x] Modificado `ably.service.ts` para usar `Ably.Rest` y el método `createTokenRequest`.
  - [x] Modificado `ably.module.ts` para incluir `AblyController`.
- [x] T-AUTH-CLERK-01.5.1: Configuración del Webhook en Clerk (Manual).
  - [x] Configuración del webhook en el Dashboard de Clerk.
  - [x] Selección de eventos (`user.created`, `user.updated`, `user.deleted`).
  - [x] Resguardo del Webhook Secret.
- [x] T-AUTH-CLERK-01.5.2: Configuración del Entorno en NestJS.
  - [x] Añadido `CLERK_WEBHOOK_SECRET` al archivo `.env` (localmente).
  - [x] Instalado `svix`.
  - [x] Verificado `ConfigModule` en `app.module.ts`.
- [x] Corrección de errores de ESLint en archivos de prueba.
  - [x] Renombrado `module` a `testModule` en `ably.service.spec.ts` y `clerk.strategy.spec.ts`.
  - [x] Corregido error de parsing en `clerk.strategy.spec.ts`.
- [x] Resolución de problemas de Git.
  - [x] Commit fallido por `pre-commit` hook (ESLint).
  - [x] Commit fallido por `commitlint` (scope inválido).
  - [x] `git push` fallido por falta de upstream.
  - [x] Creación de PR, merge y borrado de rama remota (manual por el usuario).
  - [x] Actualización de `develop` local y borrado de rama local.

## 🔍 Problemas / Bloqueos

- Error al añadir .gitignore inicialmente debido a la ruta incorrecta. Corregido.
- Error de commitlint al intentar commitear con un scope incorrecto. Corregido.
- Errores de TypeScript en `clerk.strategy.ts` relacionados con la tipificación del payload de Clerk JWT. Resueltos mediante la redefinición de `CustomClerkJWTClaims` y la corrección de la importación.
- `ERR_PNPM_FETCH_404` para `@types/svix` durante la instalación de dependencias. Resuelto instalando solo `svix` ya que incluye sus propios tipos.
- Archivo `.env` ignorado por `.gitignore`, impidiendo su commit. Se decidió no commitear credenciales sensibles.

## 🚀 Próximos pasos

- T-AUTH-CLERK-01.5.3: Creación del Módulo y Controlador de Webhook.

## 🧠 Notas / decisiones / aprendizajes

- Es crucial verificar la ruta de ejecución de los comandos `git` cuando se trabaja con submódulos.
- Siempre revisar las reglas de `commitlint` para asegurar la consistencia de los mensajes de commit.
- La importancia de verificar si los paquetes de tipos (`@types/`) son realmente necesarios o si la librería ya los incluye.
- No commitear archivos `.env` con credenciales sensibles.
